---
import '../styles/global.css'

import Header from '../components/Header.astro'
import Footer from '../components/Footer.astro'
import ViewTransitions from '../components/ViewTransitions.astro'
import { ClientRouter } from 'astro:transitions'
---

<!-- <script src="../effects/Theatre"></script> -->
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,
    initial-scale=1" />
    <title>The portfolio of Jeff Miller</title>
    <meta name="color-scheme" content="dark light" />
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Newsreader&display=swap');
    </style>
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,700,0,0&icon_names=arrow_back_ios,open_in_new"
    />
    <!-- <ClientRouter /> -->
    <ViewTransitions />
    <!-- <script>
      const prefersDark = window.matchMedia('(prefers-color-scheme: dark)')

      if (prefersDark) {
        document.documentElement.classList.add('dark')
      } else {
        document.documentElement.classList.add('light')
      }
    </script> -->
  </head>
  <body>
    <Header transition:name="header" />
    <main>
      <slot />
    </main>
    <Footer />
  </body>
</html>

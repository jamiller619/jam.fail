---
import Main from '../layouts/Main.astro'
import Hero from '../components/Hero.astro'
import ProjectCard from '../components/ProjectCard.astro'
import Content from '../components/Content.astro'
// import { getCollection } from 'astro:content'

// const projects = await getCollection('projects')
---

<Main>
  <Hero />
  <Content collection="projects" />
  <!-- <div class="grid">
    {projects.map((project) => <ProjectCard {...project.data} />)}
  </div> -->
</Main>

<!-- <script>
  import { animate } from 'animejs'
  import { wrapText, type Options } from '../effects/TextWrapper'

  const opts: Options = {
    attributes: {
      style: 'display: inline-block; transform: translateY(30px)',
    },
  }

  document.querySelectorAll('.link h2').forEach((el) => wrapText(el, opts))

  document.querySelectorAll('.link').forEach((el) => {
    const targets = el.querySelectorAll('span')

    const enter = animate(targets, {
      translateY: 0,
      easing: 'easeOutExpo',
      duration: 320,
      delay: (el, i) => 40 * i,
      autoplay: false,
    })

    el.addEventListener('mouseover', () => enter.restart())
    el.addEventListener('mouseout', () => enter.reverse())
    el.addEventListener('focus', () => enter.restart())
    el.addEventListener('blur', () => enter.reverse())
  })
</script> -->
